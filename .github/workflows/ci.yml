name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Cargo fmt
        run: cargo fmt -- --check
      - name: Cargo clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Cargo test
        run: cargo test

  macos-bundle:
    runs-on: macos-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install tauri-cli
        run: cargo install tauri-cli --version 2.5.1 --locked
      - name: Build Tauri bundle (unsigned)
        working-directory: gui
        run: cargo tauri build --ci --verbose
      - name: Notarize (if secrets provided)
        if: env.APPLE_ID != '' && env.APPLE_TEAM_ID != '' && env.APPLE_PASSWORD != ''
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
        run: |
          DMG=$(ls gui/src-tauri/target/release/bundle/dmg/*.dmg | head -n1)
          xcrun notarytool submit "$DMG" --apple-id "$APPLE_ID" --team-id "$APPLE_TEAM_ID" --password "$APPLE_PASSWORD" --wait
          xcrun stapler staple "$DMG"
      - name: Upload bundle
        uses: actions/upload-artifact@v4
        with:
          name: pocket-macos-bundle
          path: |
            gui/src-tauri/target/release/bundle/macos/*.app
            gui/src-tauri/target/release/bundle/dmg/*.dmg

  windows-bundle:
    runs-on: windows-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install tauri-cli
        run: cargo install tauri-cli --version 2.5.1 --locked
      - name: Build Tauri bundle (unsigned)
        working-directory: gui
        run: cargo tauri build --ci --verbose
      - name: Upload bundle
        uses: actions/upload-artifact@v4
        with:
          name: pocket-windows-bundle
          path: |
            gui/src-tauri/target/release/bundle/nsis/*.exe
            gui/src-tauri/target/release/bundle/msi/*.msi
